/// <reference types="react" />
import { BoardProps } from './Board.types';
export declare const Board: ({ columns, setColumns, title, projectId, _id, createdAt, updatedAt, i18n, emitter, editList }: BoardProps) => JSX.Element;
