/// <reference types="react" />
import { HeaderProps } from './Header.types';
declare const Header: ({ text, color, itemCount, dragHandleProps, _deleteColumn, _editColumn, columnIndex, isDragging, i18n, newColumn, setNewColumn, locked, _isEditingColumn, }: HeaderProps) => JSX.Element;
export default Header;
