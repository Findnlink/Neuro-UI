import React from 'react'
import { render } from '@testing-library/react'

import { Icon } from './Icon'
import { IconProps } from './Icon.types'

describe('Icon Component', () => {
  const renderComponent = () => render(<Icon icon={'add'} color="black" />)

  it('', () => {})
})
