import { ErrorMessage, ComponentWrapper } from '../'

# ErrorMessage

<ComponentWrapper
  options={[
    { type: 'bool', name: 'isVisible', default: true },
    { type: 'bool', name: 'loading', default: false },
    {
      type: 'array',
      name: 'scale',
      default: 'm',
      values: ['s', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'weight',
      default: 'medium',
      values: ['light', 'medium', 'bold']
    },
    {
      type: 'array',
      name: 'padding',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'margin',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'string',
      name: 'children',
      default: 'Error! Something went wrong'
    }
  ]}
>
  <ErrorMessage />
</ComponentWrapper>

Hallo
