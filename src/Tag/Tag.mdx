import { Tag, ComponentWrapper } from '../'

# Tag

<ComponentWrapper
  options={[
    { type: 'bool', name: 'primary', default: false },
    { type: 'bool', name: 'disabled', default: false },
    { type: 'bool', name: 'loading', default: false },
    {
      type: 'array',
      name: 'scale',
      default: 'm',
      values: ['s', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'weight',
      default: 'medium',
      values: ['light', 'medium', 'bold']
    },
    {
      type: 'array',
      name: 'padding',
      default: 's m',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'margin',
      default: 's',
      values: ['0', 's', 'm', 'l', 'xl']
    }
  ]}
>
  <Tag href={['', 'https://www.findnlink.com', '']}>
    {['Neuro UI', 'www.findnlink.com', 'Tag3']}
  </Tag>
</ComponentWrapper>
