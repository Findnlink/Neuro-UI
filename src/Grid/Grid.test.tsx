import React from "react";
import { render } from "@testing-library/react";

import { Grid } from "./Grid";
import { GridProps } from "./Grid.types";

describe("Grid Component", () => {
  const renderComponent = () => render(<Grid foo={"foo"} />);

  it("", () => {
    
  });
});
