import { Tabs, ComponentWrapper } from '../'

# Tabs

<ComponentWrapper
  options={[
    { type: 'bool', name: 'primary', default: false },
    { type: 'bool', name: 'disabled', default: false },
    { type: 'bool', name: 'loading', default: false },
    { type: 'bool', name: 'hover', default: false },
    {
      type: 'array',
      name: 'scale',
      default: 'm',
      values: ['s', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'weight',
      default: 'medium',
      values: ['light', 'medium', 'bold']
    },
    {
      type: 'array',
      name: 'padding',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'margin',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    }
  ]}
>
  <Tabs tabs={['Tab1', 'Tab2tab', 'Tab3', 'Tab4tabtabtab']}>
    {['test', 'test2', 'test3', 'test4']}
  </Tabs>
</ComponentWrapper>

Note: If you want to use multiple tabs in the same page, you must pass an unique ID.
