import { Tabs, Tab, TabsContent, TabsHeader, ComponentWrapper } from '../'

# Tabs

<ComponentWrapper
  options={[
    { type: 'bool', name: 'primary', default: false },
    { type: 'bool', name: 'disabled', default: false },
    { type: 'bool', name: 'loading', default: false },
    { type: 'bool', name: 'hover', default: false },
    {
      type: 'array',
      name: 'scale',
      default: 'm',
      values: ['s', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'weight',
      default: 'medium',
      values: ['light', 'medium', 'bold']
    },
    {
      type: 'array',
      name: 'padding',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'margin',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'direction',
      default: 'row',
      values: ['row', 'column']
    }
  ]}
>
  <Tabs>
    <TabsHeader>
      <Tab index={0}>Tab1</Tab>
      <Tab index={1}>Tab2</Tab>
      <Tab index={2}>Tab3</Tab>
      <Tab index={3}>Tab4</Tab>
    </TabsHeader>
    <TabsContent>{['Tab1', 'Tab2']}</TabsContent>
  </Tabs>
</ComponentWrapper>

Note: If you want to use multiple tabs in the same page, you must pass an unique ID.
