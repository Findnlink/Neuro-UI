import react, { useState } from 'react'
import { Board, ComponentWrapper } from '../../'
import _data from './data.json'

# Introduction

## What is NeuroUI (nui)?

React based Component System

## Vision

To create an simple ui

## Roadmap

export const BoardComponent = () => {
  const [data, setData] = useState(_data[0])
  const [editList, setEditList] = useState([])
  return (
    <ComponentWrapper
      propsUnderComponent
      options={[{ type: 'string', name: 'title', default: '' }]}
    >
      <Board
        _id={data._id}
        columns={data.columns}
        setColumns={setData}
        title={data.title}
        projectId={data.projectId}
        createdAt={data.createdAt}
        updatedAt={data.updatedAt}
        i18n={{ addNew: 'Add new', remove: 'Remove', edit: 'Edit' }}
        editList={editList}
      />
    </ComponentWrapper>
  )
}

<BoardComponent />

<BoardComponent />

<BoardComponent />

<BoardComponent />
