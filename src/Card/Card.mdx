import { Card, ComponentWrapper } from '../'

# Card

<ComponentWrapper
  options={[
    { type: 'bool', name: 'disabled', default: false },
    { type: 'bool', name: 'loading', default: false },
    {
      type: 'array',
      name: 'scale',
      default: 'm',
      values: ['s', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'weight',
      default: 'medium',
      values: ['light', 'medium', 'bold']
    },
    {
      type: 'array',
      name: 'padding',
      default: 'xl',
      values: ['0', 's', 'm', 'l', 'xl']
    },
    {
      type: 'array',
      name: 'margin',
      default: 'm',
      values: ['0', 's', 'm', 'l', 'xl']
    }
  ]}
>
  <Card>Hello</Card>
</ComponentWrapper>
