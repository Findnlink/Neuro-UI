import React from "react";
import { render } from "@testing-library/react";

import { Logo } from "./Logo";
import { LogoProps } from "./Logo.types";

describe("Logo Component", () => {
  const renderComponent = () => render(<Logo foo={"foo"} />);

  it("", () => {
    
  });
});
